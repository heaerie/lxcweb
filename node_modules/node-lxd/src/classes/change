				container._client._request("GET /containers/" + container._metadata.name + "/snapshots", {}, function(err, metadata) {
				    if (err) {
					callback(err);
				    } else {
					container._metadata.snapshots = metadata;
					callback(err, container);
				    }
				});
